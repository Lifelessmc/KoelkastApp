<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="koelkastapp.projectgroepje4.com.koelkastapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".KoelkastActivity" />
        <activity android:name=".VriezerActivity" />
        <activity android:name=".KastActivity" />
        <activity android:name=".BoodschappenlijstActivity" />
        <activity android:name=".AddToKoelkastActivity"></activity>
    </application>

</manifest>